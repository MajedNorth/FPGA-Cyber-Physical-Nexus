[options]
mode prove

[engines]
smtbmc

[script]
read -formal uart_rx.sv

# Set the parameter on the module BEFORE we prepare it
chparam -set CLKS_PER_BIT 5 uart_rx

# Now prepare the design (this will see the change we just made)
prep -top uart_rx

[files]
uart_rx.sv